# 株式会社えんのした ウェブサイト改善プラン

## 3段階マルチエージェントレビュー最終報告書

**作成日**: 2026年3月2日
**対象**: WordPressテーマ `ennoshita` v4.1
**レビュー手法**: 3段階の独立レビュー（トップレベルSE分析 → 批判的第三者監査 → 大御所アーキテクト最終検証）

---

## 総合評価

| カテゴリ | 評価 | コメント |
|----------|------|----------|
| コード品質 | ★★★★☆ | セキュリティ・エスケープ処理は優秀。ただし構造の肥大化あり |
| デザイン/UI | ★★★☆☆ | 基盤は整っているが、画像アセット不足で「仮サイト」感 |
| SEO | ★★★★☆ | JSON-LD/OGP/canonical実装済み。構造化データに不完全あり |
| パフォーマンス | ★★★☆☆ | CSS80KB未ミニファイ、日本語フォント3ファミリー10ウェイト |
| アクセシビリティ | ★★★★☆ | skip link/ARIA/prefers-reduced-motion対応。一部コントラスト懸念 |
| コンバージョン設計 | ★★☆☆☆ | リードマグネット不在、CTA単調、ファネル未設計 |
| 法的コンプライアンス | ★★☆☆☆ | Cookie同意不完全、個人情報同意チェックなし |
| 保守性 | ★★★☆☆ | functions.php肥大化、セクション番号崩壊 |

---

## 検出された問題一覧（深刻度順）

### 致命的（即時修正必須）

#### 1. 未定義関数の呼び出し（Fatal Error）
- **ファイル**: `functions.php:815`
- **問題**: `ennoshita_register_team_member_cpt()` を呼び出しているが、実際の関数名は `ennoshita_register_team_cpt()`（1032行）
- **影響**: テーマ有効化時にPHP Fatal Errorが発生し、サイトがクラッシュする
- **対策**: 関数名を `ennoshita_register_team_cpt()` に修正

#### 2. 実績セクション（数字）の完全欠落
- **ファイル**: `front-page.php`
- **問題**: セクション3（実績・数字）がテンプレートから完全に削除されている。セクション番号が1→2→4と飛んでいる
- **影響**: B2Bサイトにおいて最も重要な信頼材料（「100社以上」「満足度95%」等）がファーストビュー近辺にない。CSSの`.trust__grid`やJSの数字カウントアップ機能は実装済みなのに使われていない
- **対策**: 実績セクションを復活。数字カウントアップアニメーション（`[data-count]`）と組み合わせて実装

#### 3. お問い合わせフォームに個人情報保護同意チェックなし
- **ファイル**: `page-contact.php:58-104`
- **問題**: フォールバックフォームにプライバシーポリシーへの同意チェックボックスがない
- **影響**: 日本の個人情報保護法上、個人情報取得時には利用目的の明示と同意が必要。法的リスク
- **対策**: 送信ボタンの前にチェックボックスを追加: 「プライバシーポリシーに同意の上、送信してください」

#### 4. Cookie同意がGA4/GTMの読み込みを制御していない
- **ファイル**: `footer.php:105-115`, `functions.php:722-738`
- **問題**: Cookie同意バナーに「拒否」ボタンがなく、GA4/GTMは同意状態に関わらず`wp_head`で即座に読み込まれる
- **影響**: 改正電気通信事業法（2023年施行）およびGDPR非準拠
- **対策**:
  - 「拒否」ボタンを追加
  - GA4/GTMスクリプトをCookie同意後にのみ読み込むよう制御
  - サーバーサイドでの同意記録を検討

---

### 高（早期対応推奨）

#### 5. ヒーロー画像のカスタマイザー設定が反映されない
- **ファイル**: `front-page.php`, `functions.php:525-529`
- **問題**: カスタマイザーに `ennoshita_hero_image` 設定があるが、テンプレートでこの設定を使用するコードが存在しない
- **対策**: front-page.phpのヒーローセクションに、カスタマイザーの画像をインラインスタイルまたは`<picture>`タグで反映する処理を追加

#### 6. 画像アセットがプレースホルダーのまま
- **ファイル**: `assets/images/philosophy.svg`, `assets/images/representative.svg`
- **問題**: SVGプレースホルダーのまま納品できない
- **対策**: プロカメラマンによる撮影を手配。最低限必要な写真リスト：
  - 代表者ポートレート（400×500px縦長）
  - 研修風景2-3枚
  - オフィス/チーム写真
  - サービスイメージ各1枚

#### 7. スパム対策が皆無
- **ファイル**: `page-contact.php`, `functions.php:1184-1231`
- **問題**: honeypot、reCAPTCHA、レートリミットのいずれも未実装
- **対策**:
  - フォールバックフォームにhoneypotフィールドを追加（まずこちらを対応）
  - Contact Form 7 + reCAPTCHA v3 の導入（※プラグイン必要のため後回し）

#### 8. 事例紹介ページが存在しない
- **ファイル**: `page-service.php` にサンプルデータはあるが表示テンプレートなし
- **問題**: B2Bコンサルサイトにおいて事例紹介は最重要コンテンツ
- **対策**:
  - `case_study` カスタム投稿タイプを新設
  - `archive-case-study.php`, `single-case-study.php` テンプレートを作成
  - 課題→施策→成果の構造化テンプレート

#### 9. リードマグネット（資料ダウンロード）がない
- **問題**: 「無料相談」のみではコンバージョンのハードルが高い
- **対策**:
  - 「組織診断チェックリスト」等のPDF資料を作成
  - ダウンロードフォーム（メールアドレス取得）を設置
  - サービスページやブログ記事にCTAとして配置

#### 10. 電話番号がヘッダーにない
- **ファイル**: `header.php`
- **問題**: B2Bでは電話が最初の接点になることが多い
- **対策**: ヘッダー右上に電話番号を表示。モバイルではタップで発信可能に

---

### 中（計画的に対応）

#### 11. style.css 80KB/3099行 未ミニファイ
- **対策**:
  - 使われていないCSS（v11関連等）の削除（まずこちらを対応）
  - Critical CSSの抽出・インライン化
  - WordPress のミニファイプラグイン（Autoptimize等）導入（※プラグイン必要のため後回し）

#### 12. Google Fonts 3ファミリー10ウェイトの過剰読み込み
- **ファイル**: `functions.php:74`
- **対策**:
  - Noto Serif JP を900ウェイト1つに削減（見出しのみ使用）
  - Noto Sans JP を400/700の2ウェイトに削減
  - `&display=swap` に加え、`font-display: optional` を検討

#### 13. functions.php 1592行の肥大化
- **対策**: inc/ ディレクトリに機能別分割:
  ```
  inc/seo.php          — SEO/OGP/JSON-LD
  inc/cpt.php          — カスタム投稿タイプ
  inc/customizer.php   — カスタマイザー設定
  inc/security.php     — セキュリティ対策
  inc/analytics.php    — GA4/GTM
  inc/contact.php      — フォーム処理
  inc/nanobananapro.php — Gemini API画像生成
  ```

#### 14. JSON-LD構造化データの不完全さ
- **ファイル**: `functions.php:263-287`
- **問題**:
  - `telephone` が空文字列
  - `postalCode` フィールドが欠落（空ではなく未定義）
  - `email` フィールド未設定
  - `BreadcrumbList` スキーマ未実装
  - Article スキーマに `publisher.logo` 欠落
- **対策**: カスタマイザーの電話番号・住所を反映するよう動的化。BreadcrumbList JSON-LDを追加

#### 15. 全ページでローディングスピナー表示
- **ファイル**: `header.php:12-14`
- **問題**: 条件分岐なしで全ページに表示。再訪問ユーザーに毎回表示されるのは離脱要因
- **対策**: `is_front_page()` の場合のみ表示、または完全に削除

#### 16. CTA重複コード（DRY違反）
- **ファイル**: `single.php:160-174`, `archive.php:74-88`, `page-about.php:73-82`
- **問題**: 各テンプレートでCTAをインライン記述。`template-parts/section-cta.php` を使用していない
- **対策**: 全箇所を `get_template_part('template-parts/section', 'cta')` に統一

#### 17. page-service.php内にサンプルデータ関数
- **ファイル**: `page-service.php:397-536`
- **問題**: テンプレートファイル内に400行超の関数定義。WordPressアンチパターン
- **対策**: `ennoshita_get_service_sample_data()` を `functions.php` または `inc/service-data.php` に移動

#### 18. テスティモニアルの信憑性不足
- **ファイル**: `front-page.php:208以降`
- **問題**: デフォルト3件がハードコード。社名が「A社」「B社」「C社」、アバターも空
- **対策**: カスタム投稿タイプ `testimonial` からの動的取得を実装。実際の顧客の声を収集

---

### 低（改善推奨）

#### 19. page-about.php のインラインスタイル
- **ファイル**: `page-about.php:29, 32, 65`
- **対策**: 既存の `.philosophy__heading` クラスや `.entry-content--spaced` モディファイアを使用

#### 20. CSS の !important 乱用
- **ファイル**: `style.css:2905-2912`
- **問題**: `.message__lead` に5つの `!important` が連続
- **対策**: CSS詳細度の見直し。`.section--message .message__lead` のように親セレクタを追加

#### 21. SVGアイコンの重複
- **問題**: 矢印SVG等が10箇所以上でコピペされている
- **対策**: PHPヘルパー関数 `ennoshita_icon('arrow-right')` を作成

#### 22. blog-card全体がaタグで囲まれている
- **ファイル**: `template-parts/content-card.php:8`
- **問題**: カード内テキスト全体がリンクテキストになりスクリーンリーダーで冗長
- **対策**: リンクをタイトルのみに限定し、カード全体のクリックはJSまたはCSS `::after` で実現

#### 23. フッターテキストのカラーコントラスト
- **ファイル**: `style.css` footer関連
- **問題**: `rgba(255,255,255,0.4)` はWCAG AA基準（4.5:1）を満たさない可能性
- **対策**: 不透明度を0.6以上に変更

#### 24. functions.phpのセクション番号崩壊
- **問題**: 番号が10, 10, 14, 14, 15, 17, 18, 15, 21, 22, 23 と完全に乱れている
- **対策**: 全番号を振り直し。ただしinc/分割で自然に解消

---

## 現在の良い点（維持すべき要素）

| カテゴリ | 詳細 |
|----------|------|
| セキュリティ | nonce検証、sanitize/escape処理、XML-RPC無効化、REST API保護、セキュリティヘッダー |
| SEO基盤 | JSON-LD（LocalBusiness/Article）、OGP、canonical URL、title最適化 |
| レスポンシブ | 3段階ブレークポイント（1024/768/480px）、モバイルメニュー完備 |
| アクセシビリティ | skip link、ARIA属性、focus-visible、prefers-reduced-motion |
| カスタマイザー | ヒーロー、CTA、ロゴ（3段階優先度）、SNS、GA/GTM、会社情報 |
| カスタム投稿タイプ | testimonial、service_program、team_member |
| AI画像生成 | Gemini API連携（ナノバナナプロ）によるアイキャッチ自動生成 |
| テンプレート設計 | template-partsによる再利用設計 |
| JavaScript | バニラJS、フォーカストラップ、タッチ対応カルーセル |
| デザインシステム | CSSカスタムプロパティによる一貫したデザイントークン |

---

## 改善ロードマップ

### フェーズ1: 緊急修正（1〜3日）

| # | タスク | 重要度 | 工数 |
|---|--------|--------|------|
| 1 | `ennoshita_register_team_member_cpt()` → `ennoshita_register_team_cpt()` に修正 | 致命的 | 5分 |
| 2 | お問い合わせフォームに個人情報同意チェックボックス追加 | 致命的 | 30分 |
| 3 | Cookie同意バナーに「拒否」ボタン追加 + GA4/GTM遅延読み込み制御 | 致命的 | 2-3時間 |
| 4 | ヒーロー画像のカスタマイザー連携修正 | 高 | 30分 |
| 5 | スパム対策（honeypotフィールド追加） | 高 | 1時間 |
| 6 | ローディングスピナーをフロントページのみに限定 | 中 | 15分 |
| 7 | CTA重複箇所をtemplate-parts統一 | 中 | 30分 |

### フェーズ2: コンテンツ・コンバージョン強化（1〜2週間）

| # | タスク | 重要度 | 工数 |
|---|--------|--------|------|
| 8 | 実績セクション復活（数字カウントアップ付き） | 致命的 | 2-3時間 |
| 9 | 電話番号をヘッダーに追加 | 高 | 1時間 |
| 10 | 事例紹介カスタム投稿タイプ + テンプレート作成 | 高 | 1日 |
| 11 | 資料ダウンロードCTA設置（リードマグネット） | 高 | 1日 |
| 12 | テスティモニアルをCPTからの動的取得に変更 | 中 | 2-3時間 |
| 13 | JSON-LD完全化（telephone, postalCode, BreadcrumbList） | 中 | 2時間 |
| 14 | プロ撮影の写真素材差し替え（要手配） | 高 | 外部依存 |
| 15 | プライバシーポリシーページ作成 | 中 | 2時間 |

### フェーズ3: 技術基盤改善（2〜4週間）

| # | タスク | 重要度 | 工数 |
|---|--------|--------|------|
| 16 | functions.php → inc/ 分割リファクタリング | 中 | 半日 |
| 17 | page-service.php サンプルデータ関数移動 | 中 | 1時間 |
| 18 | CSS最適化（未使用CSS削除、Critical CSS）※ミニファイはプラグイン必要のため後回し | 中 | 半日 |
| 19 | Google Fonts ウェイト削減 | 中 | 1時間 |
| 20 | インラインスタイル解消、!important削除 | 低 | 2時間 |
| 21 | SVGアイコンヘルパー関数化 | 低 | 2時間 |
| 22 | blog-card のアクセシビリティ改善 | 低 | 1時間 |
| 23 | フッターのカラーコントラスト修正 | 低 | 15分 |
| 24 | ナノバナナプロ機能のプラグイン分離（※プラグイン必要のため後回し） | 低 | 半日 |

---

## B2Bコンサルティング業界特有の要件チェックリスト

| 要件 | 現状 | 推奨対応 |
|------|------|----------|
| 導入企業ロゴ一覧 | ✗ なし | トップページにロゴウォールを追加 |
| メディア掲載実績 | ✗ なし | 掲載メディアのロゴ・リンクをセクション追加 |
| 資格・認証表示 | ✗ なし | 保有資格をaboutページに追加 |
| セミナー/イベント情報 | ✗ なし | イベント告知セクションまたはページ |
| メルマガ登録フォーム | ✗ なし | ブログサイドバー・記事末尾に設置 |
| 代表者プロフィール詳細 | △ 部分的 | aboutページに詳細な経歴・実績を追加 |
| ~~サービス料金の目安~~ | — | ~~対応不要（料金目安のFAQは不掲載とする）~~ |
| コンバージョンファネル | ✗ なし | ブログ→メルマガ→資料DL→無料相談の設計 |

---

## 実際のウェブ上での見た目に関する指摘

### ファーストビュー
- CSSグラデーション背景のみで写真がない → 「工事中のサイト」に見える
- 「心技体」の文字装飾（`<em class="hero__title-accent">`）は効果的だが、テキストのみでは印象が弱い
- スクロールインジケーターは良い施策だが、モバイルでは非表示（`hero__scroll { display: none; }`）

### サービスセクション
- 3カラムのサービスカードは構造が整っている
- ホバーエフェクト（translateY + scale）は適切
- ただし、アイコンがSVGストロークのみで小さく、各サービスの差別化が弱い

### 代表メッセージ
- CSS定義は完備しているが、画像がSVGプレースホルダー
- 実写の代表者写真があれば非常に効果的なセクション

### テスティモニアル
- 「A社」「B社」では信頼性ゼロ
- アバターが空のグラデーション円 → 本物の声に見えない

### フッター
- 4カラムレイアウトは情報が整理されている
- SNSリンクはカスタマイザーから設定可能で良い
- ただしテキストカラーの不透明度が低すぎて読みにくい

---

## 第3段階（大御所レビュー）で追加された重要指摘

### 追加発見 A: 自動返信メールが未実装
- **ファイル**: `functions.php:1184-1231`
- **問題**: 管理者へのメール送信はあるが、問い合わせ者本人への確認メールがない
- **影響**: B2Bでは「問い合わせが届いたか」の不安がUXを大きく損なう
- **対策**: `wp_mail()` で送信者にも自動返信メールを送信

### 追加発見 B: 導入事例データが表示されていない
- **ファイル**: `page-service.php:409-443`
- **問題**: `case_studies` キーに詳細な事例データ（企業名、課題、施策、成果）が用意されているのに、HTMLテンプレートが存在しない
- **影響**: データだけ用意して表示を忘れている状態。最も惜しい見落とし
- **対策**: page-service.php内に事例表示セクションを追加

### 追加発見 C: ナノバナナプロAPIキーが平文保存
- **ファイル**: `functions.php:1537`
- **問題**: `update_option()` でGemini APIキーをDBに平文保存
- **対策**: 暗号化保存、またはプラグイン分離

### 追加発見 D: flush_rewrite_rules が2箇所で重複実行
- **ファイル**: `functions.php:812-818`, `functions.php:1588-1592`
- **問題**: `after_switch_theme` に2つの関数がフックされ重複実行
- **対策**: 1箇所に統合

### 追加発見 E: コンサルタント資格・専門性の可視化が弱い
- **問題**: `team_member` CPTは定義済みだが、トップページやサービスページにコンサルタント紹介セクションがない
- **影響**: B2Bコンサルでは「誰がやるのか」が購買決定の最大要因
- **対策**: サービスページに担当コンサルタント紹介を追加

### ~~追加発見 F: 料金体系の情報がゼロ~~
- **対応不要**: 料金目安のFAQは不掲載とする方針に決定

---

## 結論

このテーマは **技術的基盤として非常に優秀** です。セキュリティ、SEO、アクセシビリティの基本がしっかり実装されており、個人または小規模チームの制作物としては高い完成度です。

しかし、**B2Bサイトとしてのコンバージョン設計**と**コンテンツの充実**が大きく不足しています。3段階レビューで明らかになった核心は以下の3点です:

1. **信頼の可視化が不足**: 実績数字、導入事例、コンサルタント資格、パートナーロゴなど「この会社に任せて大丈夫」と思わせる材料が圧倒的に足りない
2. **リード獲得の多段構造が不在**: 「無料相談」一択では、情報収集段階の見込み客を逃している。ホワイトペーパー、セミナー、診断ツールなどの段階的CTAが必要
3. **用意されたデータ・機能が活用されていない**: CSS/JSの実績セクション、事例データ、カスタマイザー設定など、実装済みの機能が使われていない箇所が複数ある

**最優先対応**:
1. **致命的バグの修正**（未定義関数呼び出し → Fatal Error）
2. **法的リスクの解消**（個人情報同意チェック、Cookie同意バナー）
3. **実績・数字セクションの復活**（CSS/JS完成済み、HTMLを追加するだけ）
4. **画像アセットの差し替え**（プロ品質の写真）
5. **リードマグネットの設置**（コンバージョンファネル構築）

フェーズ1（緊急修正）は既存コードの小修正が中心で、**1〜3日で完了可能**です。特に実績数字セクションはHTMLを追加するだけで動作するため、最も費用対効果が高い改善です。
